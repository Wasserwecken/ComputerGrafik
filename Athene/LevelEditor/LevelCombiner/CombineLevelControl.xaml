<UserControl x:Class="LevelEditor.LevelCombiner.CombineLevelControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LevelEditor.LevelCombiner"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Margin="35">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Border BorderThickness="1" BorderBrush="Black">
            <StackPanel x:Name="StackPanelCheckbox" Grid.Row="0" Grid.Column="0" />
        </Border>
        <ContentControl x:Name="ContentControlView" Grid.Row="0" Grid.Column="1" />
        
        <Button Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Content="Zusammenfügen" x:Name="ButtonCombine" HorizontalAlignment="Center" />

    </Grid>
</UserControl>
