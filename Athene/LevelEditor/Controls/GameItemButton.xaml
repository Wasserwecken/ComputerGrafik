<UserControl x:Class="LevelEditor.Controls.GameItemButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LevelEditor.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Button  x:Name="MainButton" Style="{DynamicResource GridButtonStyle}" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch">
        <Border HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <TextBlock VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Padding="10" x:Name="TitleTextBlock" Text="Test" />
        </Border>
    </Button>
    
</UserControl>
